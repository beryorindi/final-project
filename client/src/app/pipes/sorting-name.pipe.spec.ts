import { SortingNamePipe } from './sorting-name.pipe';

describe('SortingNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SortingNamePipe();
    expect(pipe).toBeTruthy();
  });
});
